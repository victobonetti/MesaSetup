- hosts: localhost
  tasks:
    - name: Execução de provisioning
      ansible.builtin.import_role:
        name: provisioning
      tags: always

    - name: Instalação de SDK
      ansible.builtin.include_role:
        name: sdk
      tags: always

    - name: Instalação de serviços
      ansible.builtin.include_role:
        name: sdk
      tags: always
